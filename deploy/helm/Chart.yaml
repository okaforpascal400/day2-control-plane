apiVersion: v2
name: day2-control-plane
description: Demo workload for day2-control-plane — api, worker, dashboard, and Postgres.
type: application
# Chart version tracks the chart; appVersion tracks the services it deploys.
# 0.2.0: api/worker expose Prometheus /metrics; chart gains ServiceMonitor +
# PodMonitor (Phase 3 observability).
version: 0.2.0
appVersion: "0.2.0"
kubeVersion: ">=1.29.0-0"
maintainers:
  - name: Pascal Okafor
