# Runtime deps, exact pins per CLAUDE.md rule 2 (no floating versions).
# fastapi 0.139.x pulls starlette >=1.3.1, which carries the fixes for
# CVE-2025-62727, CVE-2026-48818, and CVE-2026-54283 that trivy gates on.
fastapi==0.141.1
uvicorn[standard]==0.51.0
sqlalchemy==2.0.51
asyncpg==0.31.0
pydantic==2.13.4
pydantic-settings==2.14.2
# /metrics for Prometheus (request rate/latency/errors). Pulls prometheus-client;
# constrained to starlette <2 / >=1.0, satisfied by the fastapi pin above.
prometheus-fastapi-instrumentator==8.1.0
