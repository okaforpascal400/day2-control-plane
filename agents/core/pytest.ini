[pytest]
testpaths = tests
addopts = -q
# Same convention as app/api and app/worker: state the import roots so bare
# `pytest` and `python -m pytest` behave identically. `.` puts day2_agents on
# the path; `../triage` lets the core suite import the agent it guards.
pythonpath = . ../triage
